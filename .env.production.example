# Euda Mobile — PRODUCTION environment template
# Copy to .env.production (gitignored) and fill in production values.
# For local dev against prod (read-only sanity checks): `cp .env.production .env`
# These same values are mirrored into the `production` profile in eas.json.

EXPO_PUBLIC_APP_ENV=prod

# Production Supabase project credentials
EXPO_PUBLIC_SUPABASE_URL=https://lkmntknpaiaiqvupzjbz.supabase.co
EXPO_PUBLIC_SUPABASE_ANON_KEY=<production-publishable-key>

EXPO_PUBLIC_PHONE_HASH_SALT=euda_phone_salt_2024

# Production Sentry DSN
EXPO_PUBLIC_SENTRY_DSN=<production-sentry-dsn>
